#ifndef WindowSettings_HPP
#define WindowSettings_HPP

#include <Windows.h>

const double winWidth = 1280;
const double winHeight = 720;
const char *winTitle = "FrogGame";
const unsigned long long fps = 1000 / 60; // 60 fps

HWND hwnd;

#endif